# Backend
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/grantdb
JWT_SECRET=your-secret-key-change-in-production
CORS_ORIGIN=http://localhost:4000
PORT=8080
UPLOAD_DIR=./uploads

# Frontend (build-time for Docker)
# VITE_API_URL=  leave empty for same-origin when behind same host/ALB
